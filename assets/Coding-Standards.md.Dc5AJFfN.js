import{_ as a,o as t,c as o,a2 as i}from"./chunks/framework.Dg5fKO7V.js";const u=JSON.parse('{"title":"Coding Standards","description":"","frontmatter":{},"headers":[],"relativePath":"Coding-Standards.md","filePath":"Coding-Standards.md","lastUpdated":1788563284000}'),n={name:"Coding-Standards.md"};function r(s,e,d,c,l,p){return t(),o("div",null,[...e[0]||(e[0]=[i('<h1 id="coding-standards" tabindex="-1">Coding Standards <a class="header-anchor" href="#coding-standards" aria-label="Permalink to &quot;Coding Standards&quot;">​</a></h1><p>Formatting is automated: Prettier for TypeScript, <code>rustfmt</code> for Rust, ESLint at zero warnings and Clippy with warnings denied. <code>npm run ci:local</code> is the deterministic local gate; the <a href="./SonarQube">SonarQube</a> analysis adds repository project checks, security and reliability rules, and the 80% coverage conditions.</p><h2 id="documentation" tabindex="-1">Documentation <a class="header-anchor" href="#documentation" aria-label="Permalink to &quot;Documentation&quot;">​</a></h2><p>Every TypeScript and TSX file starts with a JSDoc block containing <code>@fileoverview</code>. It explains the module&#39;s purpose and boundary, not its filename. Important exported functions and types carry JSDoc at the declaration.</p><p>Every Rust file starts with inner module documentation (<code>//!</code>). Crate-public items carry <code>///</code> Rustdoc. Public documentation describes the contract a caller can rely on. An implementation comment records a non-obvious reason or an invariant, and nothing else.</p><p>Comments do not narrate syntax, address a person, or describe the change that produced the code. They describe the code as it is. Longer architectural reasoning belongs on these pages.</p><h2 id="names" tabindex="-1">Names <a class="header-anchor" href="#names" aria-label="Permalink to &quot;Names&quot;">​</a></h2><ul><li>Use PascalCase for TSX component files and camelCase for TypeScript modules.</li><li>Use snake_case for Rust modules and functions, and PascalCase for Rust types.</li><li>Folders use kebab-case when a capability needs more than one word.</li><li>Name factories after ownership and lifecycle: <code>create...</code> or <code>install...</code>.</li><li>Do not repeat the feature name already present in the path.</li><li>Avoid structural filler such as <code>helpers</code>, <code>utils</code>, <code>service</code>, <code>controller</code>, <code>component</code>, <code>manager</code> or <code>misc</code>. Name the capability or decision instead.</li><li>Avoid abbreviations unless the protocol itself owns the abbreviation.</li><li>Single letters are reserved for genuine coordinates and the conventional i18n <code>t</code> function.</li></ul><h2 id="structure" tabindex="-1">Structure <a class="header-anchor" href="#structure" aria-label="Permalink to &quot;Structure&quot;">​</a></h2><p>Frontend feature code uses <code>ui/io/model/tests</code>. Shared frontend code uses named capability packages. Backend modules follow the subsystem map in <a href="./Architecture-Backend">Backend Architecture</a>.</p><p>One file owns one coherent subject. File size is a signal, not a rule: split when rendering, orchestration and policy are mixed, or when a section has a useful independent name and contract. Do not split a cohesive algorithm merely to satisfy a line count.</p><p>Keep parsing and policy pure. IO gathers input and applies output, model code decides. A component renders state and invokes named operations rather than embedding persistence or transport details.</p><h2 id="imports-and-dependencies" tabindex="-1">Imports and dependencies <a class="header-anchor" href="#imports-and-dependencies" aria-label="Permalink to &quot;Imports and dependencies&quot;">​</a></h2><ul><li>Use the <code>@/</code> alias for cross-package frontend imports and relative imports inside a tight local implementation only when clearer.</li><li>UI never calls Tauri directly. <code>src/shared/ipc</code> is the frontend boundary to the backend.</li><li>Feature model code does not import IO, UI, global state, media or network packages.</li><li>Shared code never imports a feature. App composes features, and nothing points back at app.</li><li><code>src/config</code> imports nothing.</li><li>No new barrel file is added merely to shorten imports. A barrel represents an intentional public package surface.</li></ul><h2 id="tests" tabindex="-1">Tests <a class="header-anchor" href="#tests" aria-label="Permalink to &quot;Tests&quot;">​</a></h2><p>Tests live with the capability they describe: <code>src/features/*/tests</code>, shared package <code>tests/</code> folders, and <code>src-tauri/src/tests</code>. Repository-wide architecture, documentation, locale and asset contracts live in <code>scripts/sonar/project-checks.mjs</code> and are imported as Sonar issues.</p><p>Prefer testing a public decision or interaction over private implementation details. Mock at the IO boundary, not halfway through pure logic. Network smoke tests are explicitly opt-in.</p><h2 id="compatibility-contracts" tabindex="-1">Compatibility contracts <a class="header-anchor" href="#compatibility-contracts" aria-label="Permalink to &quot;Compatibility contracts&quot;">​</a></h2><p>These are changed only with an explicit migration or product decision:</p><ul><li>Command and event names and payloads</li><li>Persisted schema, settings keys and avatar seeds</li><li>Provider ids and ordering</li><li>Routes and deep links</li><li>Cleanup on profile switch and shutdown</li></ul><p>Database migrations are additive. User data is not destroyed or recreated as a shortcut.</p><h2 id="interface-copy" tabindex="-1">Interface copy <a class="header-anchor" href="#interface-copy" aria-label="Permalink to &quot;Interface copy&quot;">​</a></h2><p>Both locales carry the same keys. User-facing copy carries no em dashes. Use a comma or a full stop.</p>',23)])])}const m=a(n,[["render",r]]);export{u as __pageData,m as default};
