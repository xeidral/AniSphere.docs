import{_ as a,o as t,c as o,a2 as s}from"./chunks/framework.Dg5fKO7V.js";const u=JSON.parse('{"title":"Playback and Proxy","description":"","frontmatter":{},"headers":[],"relativePath":"Playback-And-Proxy.md","filePath":"Playback-And-Proxy.md","lastUpdated":1788563284000}'),r={name:"Playback-And-Proxy.md"};function i(n,e,l,d,h,p){return t(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="playback-and-proxy" tabindex="-1">Playback and Proxy <a class="header-anchor" href="#playback-and-proxy" aria-label="Permalink to &quot;Playback and Proxy&quot;">​</a></h1><h2 id="why-a-proxy-exists" tabindex="-1">Why a proxy exists <a class="header-anchor" href="#why-a-proxy-exists" aria-label="Permalink to &quot;Why a proxy exists&quot;">​</a></h2><p>A provider hands back a stream URL, usually an HLS playlist. The WebView cannot simply load it:</p><ul><li>The content security policy does not allow arbitrary third-party media hosts.</li><li>The origin expects a <code>Referer</code> and often a specific user agent, which a media element will not send.</li><li>An HLS playlist contains absolute URLs to more third-party hosts, so even a permitted playlist points at forbidden segments.</li></ul><p>So media goes through a small HTTP server on loopback, in <code>src-tauri/src/server/</code>.</p><h2 id="what-the-proxy-does" tabindex="-1">What the proxy does <a class="header-anchor" href="#what-the-proxy-does" aria-label="Permalink to &quot;What the proxy does&quot;">​</a></h2><p><code>media_proxy.rs</code>:</p><ul><li>Carries the headers the origin expects, including the referer the provider specified.</li><li>Rewrites HLS manifests so every variant and segment URL points back at the proxy. Both the master playlist and the media playlists it references are rewritten.</li><li>Forwards range requests in both directions, which is what makes seeking work rather than re-downloading from the start.</li><li>Allows a segment host only once a playlist from this session has named it, so the proxy cannot be aimed at an arbitrary address.</li></ul><p><code>images.rs</code> does the same job for artwork, with a permanent on-disk cache keyed by a hash of the URL. It refuses anything but HTTPS and blocks private and loopback hosts.</p><h2 id="resolving-an-episode" tabindex="-1">Resolving an episode <a class="header-anchor" href="#resolving-an-episode" aria-label="Permalink to &quot;Resolving an episode&quot;">​</a></h2><ol><li>The pool matches the AniList id to a provider handle. See <a href="./Provider-System">Provider System</a>.</li><li>Every provider that carries the requested track is asked for its episode list at once, and the first list that matches the episode count AniList publishes wins. Where no count is known, the fullest list wins instead, because any non-empty list would clear the bar and the quickest provider is not necessarily the one with the whole season.</li><li>Choosing an episode asks the provider for stream links.</li><li>Links are ranked, preferring HLS and higher resolution.</li><li>The chosen URL is handed to the player as a proxy URL.</li><li>If a provider fails at any step, the pool tries the next one.</li></ol><p>Nobody waits indefinitely. The race gives every provider fifteen seconds together, and whoever is still going past that is left to finish unheard, because a race lasts as long as its slowest entrant even when none of them has the episode. The whole resolution, the race and the alternate-handle ladder after it, has thirty seconds. An episode nobody carries is answered in half a minute, and the other track is usually a second away.</p><p>A refusal and an absence are different. A provider without episode nine is skipped quietly. A provider that is blocking us is taken out of rotation.</p><h2 id="the-player" tabindex="-1">The player <a class="header-anchor" href="#the-player" aria-label="Permalink to &quot;The player&quot;">​</a></h2><p><code>hls.js</code> where HLS is not native, the platform player where it is. On top of that:</p><ul><li><strong>Opening and ending skip</strong> from <a href="https://aniskip.com" target="_blank" rel="noreferrer">AniSkip</a>. The project publishes its whole database as one file, which is imported and kept locally, so a marker is a local read rather than a request. One episode carries markers from several sites submitted against several cuts of it, so the runtime each was measured against is matched against the episode being played before votes decide. The API is asked only for what the file does not cover.</li><li><strong>Auto-play next</strong>, which reads the same episode list the page used.</li><li><strong>Speed control</strong> and a track selector.</li><li><strong>A mini-player</strong> that keeps the same video element while floating.</li></ul><p>The episode rail and the full <strong>More episodes</strong> dialog share the same merged episode assets, so each row uses its own available title and still rather than repeating the series banner.</p><ul><li><strong>A pop-out window</strong>, a real always-on-top native window.</li></ul><p>The mini-player and pop-out re-parent the existing element rather than creating a second one, which is why playback does not restart when you switch. It also means every handler attached to that element has to be released by its owner on teardown, or it leaks across the transition.</p><h3 id="controls-and-shortcuts" tabindex="-1">Controls and shortcuts <a class="header-anchor" href="#controls-and-shortcuts" aria-label="Permalink to &quot;Controls and shortcuts&quot;">​</a></h3><p>The video surface does not treat every click as play or pause. The play button owns that action; clicking or dragging the timeline seeks without changing the playback state, and the volume, speed, track, help, pop-out and fullscreen controls perform only their named action.</p><p>The speaker reflects the audible volume with one, two or three waves. Muted and zero volume use a separate compact crossed-speaker mark. The slider changes volume in five-percent steps.</p><p>Player keyboard shortcuts run only without <code>Ctrl</code>, <code>Cmd</code> or <code>Alt</code>, and never while typing into a field. In particular, plain <code>K</code> toggles playback while <code>Ctrl</code>/<code>Cmd</code> + <code>K</code> remains the application search shortcut. <code>Space</code> also toggles playback, arrows seek and change volume, and the help button lists the remaining keys.</p><p>Clicking or tapping the mini-player video returns to the full watch page. Dragging the box only repositions it, and its play, pop-out, retry and close buttons do not accidentally expand it.</p><h3 id="resuming-where-you-left-off" tabindex="-1">Resuming where you left off <a class="header-anchor" href="#resuming-where-you-left-off" aria-label="Permalink to &quot;Resuming where you left off&quot;">​</a></h3><p>The position an episode resumes from is a read of its own, and opening one from continue watching navigates immediately, so the element can already be playing from zero by the time that position arrives. It is applied when it lands, as long as only the first seconds have played and you have not seeked yourself.</p><h2 id="progress" tabindex="-1">Progress <a class="header-anchor" href="#progress" aria-label="Permalink to &quot;Progress&quot;">​</a></h2><p>Progress is written as you watch, throttled, and again on pause and teardown. It is per profile and per track, so subtitled and dubbed are recorded separately for the same episode.</p><p>Pre-translating descriptions pauses while playback runs so it does not compete for bandwidth.</p>',29)])])}const y=a(r,[["render",i]]);export{u as __pageData,y as default};
