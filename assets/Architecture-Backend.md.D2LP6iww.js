import{_ as a,o as t,c as r,a2 as o}from"./chunks/framework.Dg5fKO7V.js";const u=JSON.parse('{"title":"Backend Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"Architecture-Backend.md","filePath":"Architecture-Backend.md","lastUpdated":1788563284000}'),s={name:"Architecture-Backend.md"};function i(d,e,n,c,l,h){return t(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="backend-architecture" tabindex="-1">Backend Architecture <a class="header-anchor" href="#backend-architecture" aria-label="Permalink to &quot;Backend Architecture&quot;">​</a></h1><p>Rust under <code>src-tauri/src/</code> owns persistence, remote access, media transport, downloads and native behaviour. The frontend reaches it only through typed Tauri commands.</p><h2 id="module-map" tabindex="-1">Module map <a class="header-anchor" href="#module-map" aria-label="Permalink to &quot;Module map&quot;">​</a></h2><h3 id="app" tabindex="-1"><code>app/</code> <a class="header-anchor" href="#app" aria-label="Permalink to &quot;`app/`&quot;">​</a></h3><p>Builds the Tauri application, registers state and commands, and owns tray and window lifecycle. <code>ipc.rs</code> is the complete command registration surface. Registration tests keep command declarations and the handler in sync.</p><h3 id="commands" tabindex="-1"><code>commands/</code> <a class="header-anchor" href="#commands" aria-label="Permalink to &quot;`commands/`&quot;">​</a></h3><p>The IPC boundary, grouped by subject. Commands validate and normalise input, delegate to backend subsystems and return serializable results. Reusable policy and parsing do not live here because they must remain testable without a Tauri runtime.</p><h3 id="catalog" tabindex="-1"><code>catalog/</code> <a class="header-anchor" href="#catalog" aria-label="Permalink to &quot;`catalog/`&quot;">​</a></h3><p>Imports the manami title database, which is the whole local catalogue and the only thing fetched in bulk. Everything AniList adds is fetched for the titles on a screen: <code>catalog/rankings.rs</code> asks for the four home orderings in one request and stores them, and <code>catalog/metadata/on_demand.rs</code> fills the cache for the titles a page is holding. <code>catalog/metadata/transport.rs</code> owns pacing and priority, so every caller in the process shares one request budget, and it reads what AniList states is left of it. Kitsu supplies a smaller metadata result when AniList is unavailable.</p><p><code>catalog/skip_times.rs</code> imports the published AniSkip database as one file, the way the catalogue is imported. <code>catalog/seasons.rs</code> walks a franchise and keeps the answer, serving the stored one while a day-old walk is repeated behind the reader.</p><h3 id="database" tabindex="-1"><code>database/</code> <a class="header-anchor" href="#database" aria-label="Permalink to &quot;`database/`&quot;">​</a></h3><p>SQLite schema, migrations, persisted types and query families. <code>database/discovery/</code> separates anime, artwork, featured, ranking and studio reads. Migrations are additive and columns exist before indexes that depend on them are created.</p><h3 id="providers-and-provider-pool" tabindex="-1"><code>providers/</code> and <code>provider_pool/</code> <a class="header-anchor" href="#providers-and-provider-pool" aria-label="Permalink to &quot;`providers/` and `provider_pool/`&quot;">​</a></h3><p>Each provider adapter implements the common contract. Parsing remains separate and pure. The pool owns title and handle resolution, episode and source selection, health, circuit breaking, stream length validation and failover. See <a href="./Provider-System">Provider System</a>.</p><h3 id="server" tabindex="-1"><code>server/</code> <a class="header-anchor" href="#server" aria-label="Permalink to &quot;`server/`&quot;">​</a></h3><p>The loopback server exposes controlled routes for remote images, local files and proxied media. Media playlists are rewritten so their segments remain reachable under the WebView content policy; range requests preserve seeking. Image fetching accepts HTTPS and rejects private targets.</p><h3 id="downloads" tabindex="-1"><code>downloads/</code> <a class="header-anchor" href="#downloads" aria-label="Permalink to &quot;`downloads/`&quot;">​</a></h3><p><code>queue.rs</code> schedules jobs, <code>transfer/</code> handles direct and HLS bytes, <code>ffmpeg.rs</code> assembles media, and <code>offline/</code> stores the snapshot that keeps downloaded titles browsable without a connection.</p><h3 id="integrations" tabindex="-1"><code>integrations/</code> <a class="header-anchor" href="#integrations" aria-label="Permalink to &quot;`integrations/`&quot;">​</a></h3><p>AniList account and sync behaviour, Discord Rich Presence, translated descriptions, AniSkip data and list imports. AniList tokens use the operating-system credential store, with a compatible settings fallback on systems that provide no secret store.</p><h3 id="platform" tabindex="-1"><code>platform/</code> <a class="header-anchor" href="#platform" aria-label="Permalink to &quot;`platform/`&quot;">​</a></h3><p>Target-specific modules expose one surface for Windows, Linux and unsupported targets. Unsafe code is confined to the documented Windows system calls that require it.</p><h2 id="error-and-boundary-rules" tabindex="-1">Error and boundary rules <a class="header-anchor" href="#error-and-boundary-rules" aria-label="Permalink to &quot;Error and boundary rules&quot;">​</a></h2><ul><li>Fallible application work returns <code>AppResult&lt;T&gt;</code> and a stable <code>AppError</code> code.</li><li>Data crossing IPC derives the required Serde traits.</li><li>Remote payload parsing is pure and fixture-testable.</li><li>Provider ids, command names, event names and stored keys are compatibility contracts.</li><li>Network requests use shared client identity and explicit timeouts.</li><li>Tokens and secrets are never written to logs.</li></ul><h2 id="documentation-and-tests" tabindex="-1">Documentation and tests <a class="header-anchor" href="#documentation-and-tests" aria-label="Permalink to &quot;Documentation and tests&quot;">​</a></h2><p>Every Rust source begins with an inner <code>//!</code> comment explaining the module boundary. Every crate-public function, type, constant and static has <code>///</code> documentation. A frontend Vitest guard checks this property because it can inspect the complete Rust tree quickly; <code>cargo check</code>, <code>cargo fmt --check</code> and Clippy then validate Rust itself.</p><p>Deterministic tests mirror backend subsystems under <code>src-tauri/src/tests/</code>. Tests that contact real providers or metadata services are opt-in smoke tests and are not part of the ordinary deterministic suite.</p>',27)])])}const m=a(s,[["render",i]]);export{u as __pageData,m as default};
