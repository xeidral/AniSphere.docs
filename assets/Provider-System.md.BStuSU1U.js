import{_ as t,o as a,c as r,a2 as i}from"./chunks/framework.Dg5fKO7V.js";const u=JSON.parse('{"title":"Provider System","description":"","frontmatter":{},"headers":[],"relativePath":"Provider-System.md","filePath":"Provider-System.md","lastUpdated":1788563284000}'),o={name:"Provider-System.md"};function s(d,e,n,h,c,l){return a(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="provider-system" tabindex="-1">Provider System <a class="header-anchor" href="#provider-system" aria-label="Permalink to &quot;Provider System&quot;">​</a></h1><p>A provider is one streaming site. Each lives in its own folder under <code>src-tauri/src/providers/</code>, implements one trait, and is declared once in <code>src-tauri/src/providers/mod.rs</code>. That registry is the only place a provider is named. Failover, health probing, the connectivity board and the debug selector all derive from it, so adding a source means writing the module and adding its entry.</p><h2 id="the-contract" tabindex="-1">The contract <a class="header-anchor" href="#the-contract" aria-label="Permalink to &quot;The contract&quot;">​</a></h2><p><code>src-tauri/src/providers/contract.rs</code> defines what every provider must answer:</p><table tabindex="0"><thead><tr><th>Method</th><th>Answers</th></tr></thead><tbody><tr><td><code>search</code></td><td>Titles matching a query, each with an opaque handle</td></tr><tr><td><code>episodes</code></td><td>Which episodes exist for a handle and a track</td></tr><tr><td><code>episode_sources</code></td><td>Playable stream links for one episode</td></tr><tr><td><code>episode_titles_en</code></td><td>Per-episode titles, when the site publishes them</td></tr></tbody></table><p>A handle is the provider&#39;s own identifier, usually a slug. It is opaque on purpose. Nothing outside the provider may parse it, because its shape is that site&#39;s business and changes without warning.</p><p>Some sites address a title by its AniList id instead of a slug of their own. Those set <code>handle_is_anilist_id</code>, and the pool hands them the id directly rather than searching for a title they may not list under the name the catalogue knows.</p><p><code>ProviderMetadata</code> carries the stable identity: the persisted id, the display name, which tracks it serves, whether one handle covers every season of a franchise, and where it sits on the connectivity map.</p><h2 id="tracks" tabindex="-1">Tracks <a class="header-anchor" href="#tracks" aria-label="Permalink to &quot;Tracks&quot;">​</a></h2><p>A track is a language and dub combination: <code>sub</code>, <code>dub</code>, <code>raw</code>, <code>ger-sub</code>, <code>ger-dub</code>. A provider declares which it serves, and the pool only asks it for those.</p><p>When the track you asked for has nothing, the app moves along a chain, and the chain gives up the <strong>language</strong> before it gives up the <strong>audio kind</strong>. Someone who asked for a dub asked to hear the show rather than read it, so a missing German dub is answered with an English dub, and subtitles only come up once no dub exists at all: <code>ger-dub</code> to <code>dub</code> to <code>ger-sub</code> to <code>sub</code>. A preference in English stays in English and never crosses into German, because someone watching in English would rather read English subtitles than hear a German dub.</p><h2 id="the-pool" tabindex="-1">The pool <a class="header-anchor" href="#the-pool" aria-label="Permalink to &quot;The pool&quot;">​</a></h2><p><code>src-tauri/src/provider_pool/</code> sits between the application and the providers.</p><p><strong>Title matching.</strong> The catalogue thinks in AniList ids, providers think in slugs. Matching bridges the two, and it is the hardest part. A per-season AniList entry often maps onto one multi-season provider slug: &quot;Solo Leveling Season 2&quot; becomes <code>solo-leveling</code> plus <code>staffel-2</code>, which is done by detecting the season ordinal and stripping the suffix. See <code>title_matching.rs</code>.</p><p><strong>Failover.</strong> Every eligible provider is asked at the same time, for the episode list and for the stream links alike, and the first usable answer wins. They used to be asked in turn, which meant a series nobody had a stored handle for paid a whole search per provider before the next one was even started, and that was most of the wait before a first play began. Registry order still decides ties. A refusal is not the same as an absence, and the difference matters: a provider that has no episode nine is skipped, a provider that is blocking us is removed from rotation.</p><p><strong>The circuit breaker.</strong> Repeated failures take a provider out for a while rather than retrying it on every request. <code>PROVIDER_BLOCKED</code> short-circuits that immediately.</p><p><strong>Caching.</strong> A resolved handle is remembered, and so is a negative result, for a shorter time. That second part surprises people: if a German dub has just appeared, the app may not notice for half an hour.</p><h2 id="health" tabindex="-1">Health <a class="header-anchor" href="#health" aria-label="Permalink to &quot;Health&quot;">​</a></h2><p>Every provider declares a <code>HealthProbe</code>, either resolving a known handle directly or searching for a known title first. The connectivity board runs those live from your own machine, which is why it shows what is actually reachable for you rather than what a status page claims.</p><h2 id="adding-a-provider" tabindex="-1">Adding a provider <a class="header-anchor" href="#adding-a-provider" aria-label="Permalink to &quot;Adding a provider&quot;">​</a></h2><ol><li>Create <code>src-tauri/src/providers/&lt;name&gt;/</code> with <code>mod.rs</code> and a <code>parsing.rs</code>.</li><li>Keep parsing pure. Give it HTML or JSON in and structured data out, with no network calls. That is what makes it testable without touching the site.</li><li>Implement the <code>Provider</code> trait in <code>mod.rs</code>.</li><li>Declare it in <code>src-tauri/src/providers/mod.rs</code> with its metadata block.</li><li>Add tests under <code>src-tauri/src/tests/providers/</code>, offline ones against saved fixtures and an <code>#[ignore]</code> probe for the live path. See <a href="./Testing">Testing</a>.</li></ol><p>Nothing else needs touching. If you find yourself editing the pool to special-case your provider, the contract is probably wrong and worth fixing instead.</p><h2 id="things-that-will-bite-you" tabindex="-1">Things that will bite you <a class="header-anchor" href="#things-that-will-bite-you" aria-label="Permalink to &quot;Things that will bite you&quot;">​</a></h2><ul><li><strong>AniList&#39;s episode count is not permission to play.</strong> The provider&#39;s list is the truth.</li><li><strong>Sites change their markup without notice.</strong> That is why parsing is separate and covered by fixtures, so a break is a failing test rather than a mystery.</li><li><strong>Some sites gate on TLS fingerprint</strong>, not just headers, so the HTTP client matters as much as the request.</li><li><strong>Provider ids are persisted.</strong> Changing one orphans every stored mapping.</li></ul>',24)])])}const g=t(o,[["render",s]]);export{u as __pageData,g as default};
